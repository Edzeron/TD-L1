#include <stdio.h>

int main(){
    printf("Hello World !\n");

    return 0;
}


/*
Le #include est une bibliothèque .h permettant d'importer des fonctions telles que printf(). le <...> veut dire que c'est un fichier fourni
par le compilateur (en l'occurence GCC ou Clang). stdio.h est pour standart input/output.

*/ 
